# Deployment Guide

Production deployment configurations.

## Docker Deployment

Run within container:

```bash
docker run -v $(pwd):/workspace ghcr.io/garnizeh/memex
```

## System Requirements

- Linux or macOS x86_64 / aarch64
- 512MB RAM
